# 2026-05-13 — Community catalog review fixes

Addressed the two `MANIFEST` errors flagged on the draft community-catalog page (https://community.obsidian.md/account/plugins/obsidian-book-exporter):

- `Error: The plugin ID must not contain the word "obsidian"` — `manifest.json:2`.
- `Error: Plugin description must not include the word "Obsidian"` — `manifest.json:4`.

The listing is still in **draft** mode, so renaming the plugin `id` is safe (the catalog slug locks only after acceptance). No migration shim — manual / BRAT users re-configure once.

## Changes

- `manifest.json` — `id` changed from `obsidian-book-exporter` to `book-exporter`; `description` reworded to drop the word "Obsidian" while keeping the trailing period and not starting with the plugin name:
    - Before: `Export books written in Obsidian (one manifest note + linked chapter notes) to EPUB and PDF via Pandoc.`
    - After: `Export books (one manifest note + linked chapter notes) to EPUB and PDF via Pandoc.`
- `package.json` — `name` and `description` mirrored.
- `README.md` — install path under `<vault>/.obsidian/plugins/` updated to `book-exporter`. Repo clone URL / `cd` directory left as-is (the GitHub repo name is not renamed).
- `src/app/services/exporter.ts` — temp-directory prefix changed from `obsidian-book-exporter-<slug>-` to `book-exporter-<slug>-` for consistency with the new id. Also replaced a bare `fetch()` call to download remote cover images with Obsidian's `requestUrl`, fixing a pre-existing `no-restricted-globals` lint error.
- `documentation/Architecture.md`, `documentation/plans/01-mvp.md`, `docs/configuration.md` — updated the documented temp-directory name to match.

## Untouched on purpose

- GitHub repo URLs in `CHANGELOG.md`, `package.json` (`repository.url`, `bugs.url`, `homepage`), `docs/_config.yml` (`baseurl`, GitHub link), `docs/README.md`, `docs/release-notes.md`, and previous `documentation/history/*` files — the GitHub repo itself is not renamed, only the catalog `id`.
- `documentation/history/2026-04-28.md` — historical record; describes the old temp-dir prefix at the time of writing.
- npm `keywords` in `package.json` (`obsidian`, `obsidian-plugin`) — catalog only inspects `manifest.json`; the package is `private: true`.

## Verification

- `bun run validate` — green (tsc, 37 tests pass, lint clean).
- `bun run format:check` — clean.
- `bun run build` — green.

## Dropped the release zip from GitHub Releases

Also applied the standing `documentation/plans/2026-05-13-drop-release-zip.md` plan (same change already shipped on `obsidian-update-time`, `obsidian-dataview-serializer`, `obsidian-typefully`, `obsidian-replicate`). The catalog only consumes `main.js`, `manifest.json`, and `styles.css`; the extra zip lowers the scorecard with a "release contains additional files" warning.

- `.github/workflows/release.yml` — deleted the **Create release zip** step, the **Get package name** step (only consumed by the zip line), and the `dist/${{ steps.package.outputs.name }}.zip` entry from the `softprops/action-gh-release` `files:` block. The remaining `files:` block now lists exactly the three spec assets.
- `package.json` — removed the unused `release:zip` script.
- Deleted `scripts/create-release-zip.ts` and `scripts/create-release-zip.spec.ts` (the test removal accounts for the test-count drop from 37 to 31).
- Closed the plan (`documentation/plans/2026-05-13-drop-release-zip.md` removed) — fully applied.

Verification:

- `bun run validate` — green (31 tests pass, tsc/lint clean).
- `bun run format:check` — clean.
- YAML parse of the workflow confirms exactly three release assets remain (`dist/main.js`, `dist/manifest.json`, `dist/styles.css`).

## Manual / runtime checks the agent cannot do

- After installing the new build in a vault, the plugin folder should be `<vault>/.obsidian/plugins/book-exporter/` (not `obsidian-book-exporter/`). Existing users on a manual / BRAT install need to re-enable the plugin under its new id and copy their `data.json` over, or just re-configure.
- The first GitHub release published after this change will (a) clear the two MANIFEST errors on the catalog draft page and (b) ship without the `.zip` asset, clearing the future RELEASES "additional files" warning.
