# 2026-05-14 — Community catalog review fix (timer functions)

Addressed three SOURCE CODE warnings flagged on the draft community-catalog page:

- `Use 'window.setTimeout()' instead of 'setTimeout()' for popout window compatibility.` — `src/utils/binary-probe.ts:45`.
- `Use 'window.clearTimeout()' instead of 'clearTimeout()' for popout window compatibility.` — `src/utils/binary-probe.ts:55`, `:59`.

## Changes

- `src/utils/binary-probe.ts` — replaced the bare `setTimeout` / `clearTimeout` calls in `probeBinary` with `window.setTimeout` / `window.clearTimeout`. The handle type infers to `number` (DOM) instead of Node's `Timeout`, matching how the obsidianmd lint plugin wants timer APIs to be called.

## Verification

- `bun run validate` — green (tsc, 31 tests pass, lint clean).
- `bun run format:check` — clean.
- `bun run build` — green.
